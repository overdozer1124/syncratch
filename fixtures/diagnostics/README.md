# Diagnostics fixtures

First-party fixtures for `@blocksync/diagnostics-core` live in:

`packages/diagnostics-core/src/testing/project-fixtures.ts`

Mutation oracles: `packages/diagnostics-core/src/testing/mutations.ts`

Do not scrape public Scratch projects into this tree. Teacher-submitted,
consented fixtures may be added later through a separate process.
