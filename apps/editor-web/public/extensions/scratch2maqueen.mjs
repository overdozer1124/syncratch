/* Syncratch bundled Stretch3 extension: scratch2maqueen
 * Upstream: https://github.com/champierre/scratch2maqueen.git
 * Regenerated by: node scripts/stretch3-extensions/bundle.mjs
 */

var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __commonJS = (cb, mod) => function __require() {
  try {
    return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
  } catch (e) {
    throw mod = 0, e;
  }
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));

// vendor/scratch-editor/packages/scratch-vm/src/extension-support/argument-type.js
var require_argument_type = __commonJS({
  "vendor/scratch-editor/packages/scratch-vm/src/extension-support/argument-type.js"(exports, module) {
    var ArgumentType = {
      /**
       * Numeric value with angle picker
       */
      ANGLE: "angle",
      /**
       * Boolean value with hexagonal placeholder
       */
      BOOLEAN: "Boolean",
      /**
       * Numeric value with color picker
       */
      COLOR: "color",
      /**
       * Numeric value with text field
       */
      NUMBER: "number",
      /**
       * String value with text field
       */
      STRING: "string",
      /**
       * String value with matrix field
       */
      MATRIX: "matrix",
      /**
       * MIDI note number with note picker (piano) field
       */
      NOTE: "note",
      /**
       * Inline image on block (as part of the label)
       */
      IMAGE: "image"
    };
    module.exports = ArgumentType;
  }
});

// vendor/scratch-editor/packages/scratch-vm/src/extension-support/block-type.js
var require_block_type = __commonJS({
  "vendor/scratch-editor/packages/scratch-vm/src/extension-support/block-type.js"(exports, module) {
    var BlockType = {
      /**
       * Boolean reporter with hexagonal shape
       */
      BOOLEAN: "Boolean",
      /**
       * A button (not an actual block) for some special action, like making a variable
       */
      BUTTON: "button",
      /**
       * Command block
       */
      COMMAND: "command",
      /**
       * Specialized command block which may or may not run a child branch
       * The thread continues with the next block whether or not a child branch ran.
       */
      CONDITIONAL: "conditional",
      /**
       * Specialized hat block with no implementation function
       * This stack only runs if the corresponding event is emitted by other code.
       */
      EVENT: "event",
      /**
       * Hat block which conditionally starts a block stack
       */
      HAT: "hat",
      /**
       * Specialized command block which may or may not run a child branch
       * If a child branch runs, the thread evaluates the loop block again.
       */
      LOOP: "loop",
      /**
       * General reporter with numeric or string value
       */
      REPORTER: "reporter"
    };
    module.exports = BlockType;
  }
});

// scripts/stretch3-extensions/shims/log.cjs
var require_log = __commonJS({
  "scripts/stretch3-extensions/shims/log.cjs"(exports, module) {
    module.exports = {
      info: (...args) => console.info("[scratch-vm]", ...args),
      warn: (...args) => console.warn("[scratch-vm]", ...args),
      error: (...args) => console.error("[scratch-vm]", ...args),
      debug: (...args) => console.debug("[scratch-vm]", ...args),
      silly: () => {
      },
      trace: (...args) => console.trace("[scratch-vm]", ...args)
    };
  }
});

// vendor/scratch-editor/packages/scratch-vm/src/util/color.js
var require_color = __commonJS({
  "vendor/scratch-editor/packages/scratch-vm/src/util/color.js"(exports, module) {
    var Color = class _Color {
      /**
       * @typedef {object} RGBObject - An object representing a color in RGB format.
       * @property {number} r - the red component, in the range [0, 255].
       * @property {number} g - the green component, in the range [0, 255].
       * @property {number} b - the blue component, in the range [0, 255].
       */
      /**
       * @typedef {object} HSVObject - An object representing a color in HSV format.
       * @property {number} h - hue, in the range [0-359).
       * @property {number} s - saturation, in the range [0,1].
       * @property {number} v - value, in the range [0,1].
       */
      /** @type {RGBObject} */
      static get RGB_BLACK() {
        return { r: 0, g: 0, b: 0 };
      }
      /** @type {RGBObject} */
      static get RGB_WHITE() {
        return { r: 255, g: 255, b: 255 };
      }
      /**
       * Convert a Scratch decimal color to a hex string, #RRGGBB.
       * @param {number} decimal RGB color as a decimal.
       * @returns {string} RGB color as #RRGGBB hex string.
       */
      static decimalToHex(decimal) {
        if (decimal < 0) {
          decimal += 16777215 + 1;
        }
        let hex = Number(decimal).toString(16);
        hex = `#${"000000".substr(0, 6 - hex.length)}${hex}`;
        return hex;
      }
      /**
       * Convert a Scratch decimal color to an RGB color object.
       * @param {number} decimal RGB color as decimal.
       * @returns {RGBObject} rgb - {r: red [0,255], g: green [0,255], b: blue [0,255]}.
       */
      static decimalToRgb(decimal) {
        const a = decimal >> 24 & 255;
        const r = decimal >> 16 & 255;
        const g = decimal >> 8 & 255;
        const b = decimal & 255;
        return { r, g, b, a: a > 0 ? a : 255 };
      }
      /**
       * Convert a hex color (e.g., F00, #03F, #0033FF) to an RGB color object.
       * CC-BY-SA Tim Down:
       * https://stackoverflow.com/questions/5623838/rgb-to-hex-and-hex-to-rgb
       * @param {!string} hex Hex representation of the color.
       * @returns {RGBObject} null on failure, or rgb: {r: red [0,255], g: green [0,255], b: blue [0,255]}.
       */
      static hexToRgb(hex) {
        const shorthandRegex = /^#?([a-f\d])([a-f\d])([a-f\d])$/i;
        hex = hex.replace(shorthandRegex, (m, r, g, b) => r + r + g + g + b + b);
        const result = /^#?([a-f\d]{2})([a-f\d]{2})([a-f\d]{2})$/i.exec(hex);
        return result ? {
          r: parseInt(result[1], 16),
          g: parseInt(result[2], 16),
          b: parseInt(result[3], 16)
        } : null;
      }
      /**
       * Convert an RGB color object to a hex color.
       * @param {RGBObject} rgb - {r: red [0,255], g: green [0,255], b: blue [0,255]}.
       * @returns {!string} Hex representation of the color.
       */
      static rgbToHex(rgb) {
        return _Color.decimalToHex(_Color.rgbToDecimal(rgb));
      }
      /**
       * Convert an RGB color object to a Scratch decimal color.
       * @param {RGBObject} rgb - {r: red [0,255], g: green [0,255], b: blue [0,255]}.
       * @returns {!number} Number representing the color.
       */
      static rgbToDecimal(rgb) {
        return (rgb.r << 16) + (rgb.g << 8) + rgb.b;
      }
      /**
       * Convert a hex color (e.g., F00, #03F, #0033FF) to a decimal color number.
       * @param {!string} hex Hex representation of the color.
       * @returns {!number} Number representing the color.
       */
      static hexToDecimal(hex) {
        return _Color.rgbToDecimal(_Color.hexToRgb(hex));
      }
      /**
       * Convert an HSV color to RGB format.
       * @param {HSVObject} hsv - {h: hue [0,360), s: saturation [0,1], v: value [0,1]}
       * @returns {RGBObject} rgb - {r: red [0,255], g: green [0,255], b: blue [0,255]}.
       */
      static hsvToRgb(hsv) {
        let h = hsv.h % 360;
        if (h < 0) h += 360;
        const s = Math.max(0, Math.min(hsv.s, 1));
        const v = Math.max(0, Math.min(hsv.v, 1));
        const i = Math.floor(h / 60);
        const f = h / 60 - i;
        const p = v * (1 - s);
        const q = v * (1 - s * f);
        const t = v * (1 - s * (1 - f));
        let r;
        let g;
        let b;
        switch (i) {
          default:
          case 0:
            r = v;
            g = t;
            b = p;
            break;
          case 1:
            r = q;
            g = v;
            b = p;
            break;
          case 2:
            r = p;
            g = v;
            b = t;
            break;
          case 3:
            r = p;
            g = q;
            b = v;
            break;
          case 4:
            r = t;
            g = p;
            b = v;
            break;
          case 5:
            r = v;
            g = p;
            b = q;
            break;
        }
        return {
          r: Math.floor(r * 255),
          g: Math.floor(g * 255),
          b: Math.floor(b * 255)
        };
      }
      /**
       * Convert an RGB color to HSV format.
       * @param {RGBObject} rgb - {r: red [0,255], g: green [0,255], b: blue [0,255]}.
       * @returns {HSVObject} hsv - {h: hue [0,360), s: saturation [0,1], v: value [0,1]}
       */
      static rgbToHsv(rgb) {
        const r = rgb.r / 255;
        const g = rgb.g / 255;
        const b = rgb.b / 255;
        const x = Math.min(Math.min(r, g), b);
        const v = Math.max(Math.max(r, g), b);
        let h = 0;
        let s = 0;
        if (x !== v) {
          const f = r === x ? g - b : g === x ? b - r : r - g;
          const i = r === x ? 3 : g === x ? 5 : 1;
          h = (i - f / (v - x)) * 60 % 360;
          s = (v - x) / v;
        }
        return { h, s, v };
      }
      /**
       * Linear interpolation between rgb0 and rgb1.
       * @param {RGBObject} rgb0 - the color corresponding to fraction1 <= 0.
       * @param {RGBObject} rgb1 - the color corresponding to fraction1 >= 1.
       * @param {number} fraction1 - the interpolation parameter. If this is 0.5, for example, mix the two colors equally.
       * @returns {RGBObject} the interpolated color.
       */
      static mixRgb(rgb0, rgb1, fraction1) {
        if (fraction1 <= 0) return rgb0;
        if (fraction1 >= 1) return rgb1;
        const fraction0 = 1 - fraction1;
        return {
          r: fraction0 * rgb0.r + fraction1 * rgb1.r,
          g: fraction0 * rgb0.g + fraction1 * rgb1.g,
          b: fraction0 * rgb0.b + fraction1 * rgb1.b
        };
      }
    };
    module.exports = Color;
  }
});

// vendor/scratch-editor/packages/scratch-vm/src/util/cast.js
var require_cast = __commonJS({
  "vendor/scratch-editor/packages/scratch-vm/src/util/cast.js"(exports, module) {
    var Color = require_color();
    var Cast = class _Cast {
      /**
       * Scratch cast to number.
       * Treats NaN as 0.
       * In Scratch 2.0, this is captured by `interp.numArg.`
       * @param {*} value Value to cast to number.
       * @returns {number} The Scratch-casted number value.
       */
      static toNumber(value) {
        if (typeof value === "number") {
          if (Number.isNaN(value)) {
            return 0;
          }
          return value;
        }
        const n = Number(value);
        if (Number.isNaN(n)) {
          return 0;
        }
        return n;
      }
      /**
       * Scratch cast to boolean.
       * In Scratch 2.0, this is captured by `interp.boolArg.`
       * Treats some string values differently from JavaScript.
       * @param {*} value Value to cast to boolean.
       * @returns {boolean} The Scratch-casted boolean value.
       */
      static toBoolean(value) {
        if (typeof value === "boolean") {
          return value;
        }
        if (typeof value === "string") {
          if (value === "" || value === "0" || value.toLowerCase() === "false") {
            return false;
          }
          return true;
        }
        return Boolean(value);
      }
      /**
       * Scratch cast to string.
       * @param {*} value Value to cast to string.
       * @returns {string} The Scratch-casted string value.
       */
      static toString(value) {
        return String(value);
      }
      /**
       * Cast any Scratch argument to an RGB color array to be used for the renderer.
       * @param {*} value Value to convert to RGB color array.
       * @returns {Array.<number>} [r,g,b], values between 0-255.
       */
      static toRgbColorList(value) {
        const color = _Cast.toRgbColorObject(value);
        return [color.r, color.g, color.b];
      }
      /**
       * Cast any Scratch argument to an RGB color object to be used for the renderer.
       * @param {*} value Value to convert to RGB color object.
       * @returns {RGBOject} [r,g,b], values between 0-255.
       */
      static toRgbColorObject(value) {
        let color;
        if (typeof value === "string" && value.substring(0, 1) === "#") {
          color = Color.hexToRgb(value);
          if (!color) color = { r: 0, g: 0, b: 0, a: 255 };
        } else {
          color = Color.decimalToRgb(_Cast.toNumber(value));
        }
        return color;
      }
      /**
       * Determine if a Scratch argument is a white space string (or null / empty).
       * @param {*} val value to check.
       * @returns {boolean} True if the argument is all white spaces or null / empty.
       */
      static isWhiteSpace(val) {
        return val === null || typeof val === "string" && val.trim().length === 0;
      }
      /**
       * Compare two values, using Scratch cast, case-insensitive string compare, etc.
       * In Scratch 2.0, this is captured by `interp.compare.`
       * @param {*} v1 First value to compare.
       * @param {*} v2 Second value to compare.
       * @returns {number} Negative number if v1 < v2; 0 if equal; positive otherwise.
       */
      static compare(v1, v2) {
        let n1 = Number(v1);
        let n2 = Number(v2);
        if (n1 === 0 && _Cast.isWhiteSpace(v1)) {
          n1 = NaN;
        } else if (n2 === 0 && _Cast.isWhiteSpace(v2)) {
          n2 = NaN;
        }
        if (isNaN(n1) || isNaN(n2)) {
          const s1 = String(v1).toLowerCase();
          const s2 = String(v2).toLowerCase();
          if (s1 < s2) {
            return -1;
          } else if (s1 > s2) {
            return 1;
          }
          return 0;
        }
        if (n1 === Infinity && n2 === Infinity || n1 === -Infinity && n2 === -Infinity) {
          return 0;
        }
        return n1 - n2;
      }
      /**
       * Determine if a Scratch argument number represents a round integer.
       * @param {*} val Value to check.
       * @returns {boolean} True if number looks like an integer.
       */
      static isInt(val) {
        if (typeof val === "number") {
          if (isNaN(val)) {
            return true;
          }
          return val === parseInt(val, 10);
        } else if (typeof val === "boolean") {
          return true;
        } else if (typeof val === "string") {
          return val.indexOf(".") < 0;
        }
        return false;
      }
      static get LIST_INVALID() {
        return "INVALID";
      }
      static get LIST_ALL() {
        return "ALL";
      }
      /**
       * Compute a 1-based index into a list, based on a Scratch argument.
       * Two special cases may be returned:
       * LIST_ALL: if the block is referring to all of the items in the list.
       * LIST_INVALID: if the index was invalid in any way.
       * @param {*} index Scratch arg, including 1-based numbers or special cases.
       * @param {number} length Length of the list.
       * @param {boolean} acceptAll Whether it should accept "all" or not.
       * @returns {(number|string)} 1-based index for list, LIST_ALL, or LIST_INVALID.
       */
      static toListIndex(index, length, acceptAll) {
        if (typeof index !== "number") {
          if (index === "all") {
            return acceptAll ? _Cast.LIST_ALL : _Cast.LIST_INVALID;
          }
          if (index === "last") {
            if (length > 0) {
              return length;
            }
            return _Cast.LIST_INVALID;
          } else if (index === "random" || index === "any") {
            if (length > 0) {
              return 1 + Math.floor(Math.random() * length);
            }
            return _Cast.LIST_INVALID;
          }
        }
        index = Math.floor(_Cast.toNumber(index));
        if (index < 1 || index > length) {
          return _Cast.LIST_INVALID;
        }
        return index;
      }
    };
    module.exports = Cast;
  }
});

// scripts/stretch3-extensions/shims/format-message-setup.cjs
var require_format_message_setup = __commonJS({
  "scripts/stretch3-extensions/shims/format-message-setup.cjs"(exports, module) {
    var formatMessage = require_format_message_setup();
    try {
      formatMessage.setup({
        locale: "ja",
        missingTranslation: "ignore"
      });
    } catch {
    }
    module.exports = formatMessage;
  }
});

// vendor/scratch-editor/packages/scratch-vm/src/util/jsonrpc.js
var require_jsonrpc = __commonJS({
  "vendor/scratch-editor/packages/scratch-vm/src/util/jsonrpc.js"(exports, module) {
    var JSONRPC = class {
      constructor() {
        this._requestID = 0;
        this._openRequests = {};
      }
      /**
       * Make an RPC request and retrieve the result.
       * @param {string} method - the remote method to call.
       * @param {object} params - the parameters to pass to the remote method.
       * @returns {Promise} - a promise for the result of the call.
       */
      sendRemoteRequest(method, params) {
        const requestID = this._requestID++;
        const promise = new Promise((resolve, reject) => {
          this._openRequests[requestID] = { resolve, reject };
        });
        this._sendRequest(method, params, requestID);
        return promise;
      }
      /**
       * Make an RPC notification with no expectation of a result or callback.
       * @param {string} method - the remote method to call.
       * @param {object} params - the parameters to pass to the remote method.
       */
      sendRemoteNotification(method, params) {
        this._sendRequest(method, params);
      }
      /**
       * Handle an RPC request from remote, should return a result or Promise for result, if appropriate.
       * @param {string} method - the method requested by the remote caller.
       * @param {object} params - the parameters sent with the remote caller's request.
       */
      didReceiveCall() {
        throw new Error("Must override didReceiveCall");
      }
      _sendMessage() {
        throw new Error("Must override _sendMessage");
      }
      _sendRequest(method, params, id) {
        const request = {
          jsonrpc: "2.0",
          method,
          params
        };
        if (id !== null) {
          request.id = id;
        }
        this._sendMessage(request);
      }
      _handleMessage(json) {
        if (json.jsonrpc !== "2.0") {
          throw new Error(`Bad or missing JSON-RPC version in message: ${json}`);
        }
        if (Object.prototype.hasOwnProperty.call(json, "method")) {
          this._handleRequest(json);
        } else {
          this._handleResponse(json);
        }
      }
      _sendResponse(id, result, error) {
        const response = {
          jsonrpc: "2.0",
          id
        };
        if (error) {
          response.error = error;
        } else {
          response.result = result || null;
        }
        this._sendMessage(response);
      }
      _handleResponse(json) {
        const { result, error, id } = json;
        const openRequest = this._openRequests[id];
        delete this._openRequests[id];
        if (openRequest) {
          if (error) {
            openRequest.reject(error);
          } else {
            openRequest.resolve(result);
          }
        }
      }
      _handleRequest(json) {
        const { method, params, id } = json;
        const rawResult = this.didReceiveCall(method, params);
        if (id !== null && typeof id !== "undefined") {
          Promise.resolve(rawResult).then(
            (result) => {
              this._sendResponse(id, result);
            },
            (error) => {
              this._sendResponse(id, null, error);
            }
          );
        }
      }
    };
    module.exports = JSONRPC;
  }
});

// vendor/scratch-editor/packages/scratch-vm/src/io/ble.js
var require_ble = __commonJS({
  "vendor/scratch-editor/packages/scratch-vm/src/io/ble.js"(exports, module) {
    var JSONRPC = require_jsonrpc();
    var BLE = class extends JSONRPC {
      /**
       * A BLE peripheral socket object.  It handles connecting, over web sockets, to
       * BLE peripherals, and reading and writing data to them.
       * @param {Runtime} runtime - the Runtime for sending/receiving GUI update events.
       * @param {string} extensionId - the id of the extension using this socket.
       * @param {object} peripheralOptions - the list of options for peripheral discovery.
       * @param {object} connectCallback - a callback for connection.
       * @param {object} resetCallback - a callback for resetting extension state.
       */
      constructor(runtime, extensionId, peripheralOptions, connectCallback, resetCallback = null) {
        super();
        this._socket = runtime.getScratchLinkSocket("BLE");
        this._socket.setOnOpen(this.requestPeripheral.bind(this));
        this._socket.setOnClose(this.handleDisconnectError.bind(this));
        this._socket.setOnError(this._handleRequestError.bind(this));
        this._socket.setHandleMessage(this._handleMessage.bind(this));
        this._sendMessage = this._socket.sendMessage.bind(this._socket);
        this._availablePeripherals = {};
        this._connectCallback = connectCallback;
        this._connected = false;
        this._characteristicDidChangeCallback = null;
        this._resetCallback = resetCallback;
        this._discoverTimeoutID = null;
        this._extensionId = extensionId;
        this._peripheralOptions = peripheralOptions;
        this._runtime = runtime;
        this._socket.open();
      }
      /**
       * Request connection to the peripheral.
       * If the web socket is not yet open, request when the socket promise resolves.
       */
      requestPeripheral() {
        this._availablePeripherals = {};
        if (this._discoverTimeoutID) {
          window.clearTimeout(this._discoverTimeoutID);
        }
        this._discoverTimeoutID = window.setTimeout(this._handleDiscoverTimeout.bind(this), 15e3);
        this.sendRemoteRequest("discover", this._peripheralOptions).catch((e) => {
          this._handleRequestError(e);
        });
      }
      /**
       * Try connecting to the input peripheral id, and then call the connect
       * callback if connection is successful.
       * @param {number} id - the id of the peripheral to connect to
       */
      connectPeripheral(id) {
        this.sendRemoteRequest("connect", { peripheralId: id }).then(() => {
          this._connected = true;
          this._runtime.emit(this._runtime.constructor.PERIPHERAL_CONNECTED);
          this._connectCallback();
        }).catch((e) => {
          this._handleRequestError(e);
        });
      }
      /**
       * Close the websocket.
       */
      disconnect() {
        if (this._connected) {
          this._connected = false;
        }
        if (this._socket.isOpen()) {
          this._socket.close();
        }
        if (this._discoverTimeoutID) {
          window.clearTimeout(this._discoverTimeoutID);
        }
        this._runtime.emit(this._runtime.constructor.PERIPHERAL_DISCONNECTED);
      }
      /**
       * @returns {bool} whether the peripheral is connected.
       */
      isConnected() {
        return this._connected;
      }
      /**
       * Start receiving notifications from the specified ble service.
       * @param {number} serviceId - the ble service to read.
       * @param {number} characteristicId - the ble characteristic to get notifications from.
       * @param {object} onCharacteristicChanged - callback for characteristic change notifications.
       * @returns {Promise} - a promise from the remote startNotifications request.
       */
      startNotifications(serviceId, characteristicId, onCharacteristicChanged = null) {
        const params = {
          serviceId,
          characteristicId
        };
        this._characteristicDidChangeCallback = onCharacteristicChanged;
        return this.sendRemoteRequest("startNotifications", params).catch((e) => {
          this.handleDisconnectError(e);
        });
      }
      /**
       * Read from the specified ble service.
       * @param {number} serviceId - the ble service to read.
       * @param {number} characteristicId - the ble characteristic to read.
       * @param {boolean} optStartNotifications - whether to start receiving characteristic change notifications.
       * @param {object} onCharacteristicChanged - callback for characteristic change notifications.
       * @returns {Promise} - a promise from the remote read request.
       */
      read(serviceId, characteristicId, optStartNotifications = false, onCharacteristicChanged = null) {
        const params = {
          serviceId,
          characteristicId
        };
        if (optStartNotifications) {
          params.startNotifications = true;
        }
        if (onCharacteristicChanged) {
          this._characteristicDidChangeCallback = onCharacteristicChanged;
        }
        return this.sendRemoteRequest("read", params).catch((e) => {
          this.handleDisconnectError(e);
        });
      }
      /**
       * Write data to the specified ble service.
       * @param {number} serviceId - the ble service to write.
       * @param {number} characteristicId - the ble characteristic to write.
       * @param {string} message - the message to send.
       * @param {string} encoding - the message encoding type.
       * @param {boolean} withResponse - if true, resolve after peripheral's response.
       * @returns {Promise} - a promise from the remote send request.
       */
      write(serviceId, characteristicId, message, encoding = null, withResponse = null) {
        const params = { serviceId, characteristicId, message };
        if (encoding) {
          params.encoding = encoding;
        }
        if (withResponse !== null) {
          params.withResponse = withResponse;
        }
        return this.sendRemoteRequest("write", params).catch((e) => {
          this.handleDisconnectError(e);
        });
      }
      /**
       * Handle a received call from the socket.
       * @param {string} method - a received method label.
       * @param {object} params - a received list of parameters.
       * @returns {object} - optional return value.
       */
      didReceiveCall(method, params) {
        switch (method) {
          case "didDiscoverPeripheral":
            this._availablePeripherals[params.peripheralId] = params;
            this._runtime.emit(
              this._runtime.constructor.PERIPHERAL_LIST_UPDATE,
              this._availablePeripherals
            );
            if (this._discoverTimeoutID) {
              window.clearTimeout(this._discoverTimeoutID);
            }
            break;
          case "userDidPickPeripheral":
            this._availablePeripherals[params.peripheralId] = params;
            this._runtime.emit(
              this._runtime.constructor.USER_PICKED_PERIPHERAL,
              this._availablePeripherals
            );
            if (this._discoverTimeoutID) {
              window.clearTimeout(this._discoverTimeoutID);
            }
            break;
          case "userDidNotPickPeripheral":
            this._runtime.emit(
              this._runtime.constructor.PERIPHERAL_SCAN_TIMEOUT
            );
            if (this._discoverTimeoutID) {
              window.clearTimeout(this._discoverTimeoutID);
            }
            break;
          case "characteristicDidChange":
            if (this._characteristicDidChangeCallback) {
              this._characteristicDidChangeCallback(params.message);
            }
            break;
          case "ping":
            return 42;
        }
      }
      /**
       * Handle an error resulting from losing connection to a peripheral.
       *
       * This could be due to:
       * - battery depletion
       * - going out of bluetooth range
       * - being powered down
       *
       * Disconnect the socket, and if the extension using this socket has a
       * reset callback, call it. Finally, emit an error to the runtime.
       */
      handleDisconnectError() {
        if (!this._connected) return;
        this.disconnect();
        if (this._resetCallback) {
          this._resetCallback();
        }
        this._runtime.emit(this._runtime.constructor.PERIPHERAL_CONNECTION_LOST_ERROR, {
          message: `Scratch lost connection to`,
          extensionId: this._extensionId
        });
      }
      _handleRequestError() {
        this._runtime.emit(this._runtime.constructor.PERIPHERAL_REQUEST_ERROR, {
          message: `Scratch lost connection to`,
          extensionId: this._extensionId
        });
      }
      _handleDiscoverTimeout() {
        if (this._discoverTimeoutID) {
          window.clearTimeout(this._discoverTimeoutID);
        }
        this._runtime.emit(this._runtime.constructor.PERIPHERAL_SCAN_TIMEOUT);
      }
    };
    module.exports = BLE;
  }
});

// scripts/stretch3-extensions/shims/base64-util.cjs
var require_base64_util = __commonJS({
  "scripts/stretch3-extensions/shims/base64-util.cjs"(exports, module) {
    var Base64Util = class _Base64Util {
      static uint8ArrayToBase64(array) {
        let str = "";
        for (let i = 0; i < array.length; i++) {
          str += String.fromCharCode(array[i]);
        }
        return btoa(str);
      }
      static base64ToUint8Array(base64) {
        const str = atob(base64);
        const array = new Uint8Array(str.length);
        for (let i = 0; i < str.length; i++) {
          array[i] = str.charCodeAt(i);
        }
        return array;
      }
      static arrayBufferToBase64(buffer) {
        return _Base64Util.uint8ArrayToBase64(new Uint8Array(buffer));
      }
    };
    module.exports = Base64Util;
  }
});

// .cache/stretch3-extensions/work/src/extensions/scratch3_scratch2maqueen/index.js
var require_scratch3_scratch2maqueen = __commonJS({
  ".cache/stretch3-extensions/work/src/extensions/scratch3_scratch2maqueen/index.js"(exports, module) {
    var ArgumentType = require_argument_type();
    var BlockType = require_block_type();
    var log = require_log();
    var cast = require_cast();
    var formatMessage = require_format_message_setup();
    var BLE = require_ble();
    var Base64Util = require_base64_util();
    var blockIconURI = "data:image/png;base64,";
    var BLECommand = {
      CMD_DISPLAY_TEXT: 129,
      CMD_DISPLAY_LED: 130,
      CMD_PIN_PWM: 146,
      CMD_SLOT_VALUE: 160
    };
    var BLETimeout = 4500;
    var BLESendInterval = 100;
    var BLEDataStoppedError = "micro:bit More extension stopped receiving data";
    var BLEUUID = {
      service: 61445,
      rxChar: "5261da01-fa7e-42ab-850b-7c80220097cc",
      txChar: "5261da02-fa7e-42ab-850b-7c80220097cc"
    };
    var G = 1024;
    var MicroBitMore = class {
      /**
       * Construct a MicroBit communication object.
       * @param {Runtime} runtime - the Scratch 3.0 runtime
       * @param {string} extensionId - the id of the extension
       */
      constructor(runtime, extensionId) {
        this._runtime = runtime;
        this._ble = null;
        this._runtime.registerPeripheralExtension(extensionId, this);
        this._extensionId = extensionId;
        this._sensors = {
          buttonA: 0,
          buttonB: 0,
          ledMatrixState: new Uint8Array(5),
          lightLevel: 0,
          compassHeading: 0,
          analogValue: {},
          digitalValue: {},
          slot: [0, 0, 0, 0]
        };
        this.analogIn = [0, 1, 2];
        this.analogIn.forEach((pinIndex) => {
          this._sensors.analogValue[pinIndex] = 0;
        });
        this.gpio = [
          0,
          1,
          2,
          8,
          13,
          14,
          15,
          16
        ];
        this.gpio.forEach((pinIndex) => {
          this._sensors.digitalValue[pinIndex] = 0;
        });
        this.slotLength = this._sensors.slot.length;
        this._timeoutID = null;
        this._busy = false;
        this._busyTimeoutID = null;
        this.reset = this.reset.bind(this);
        this._onConnect = this._onConnect.bind(this);
        this._onMessage = this._onMessage.bind(this);
      }
      /**
       * @param {string} text - the text to display.
       * @return {Promise} - a Promise that resolves when writing to peripheral.
       */
      displayText(text) {
        const output = new Uint8Array(text.length);
        for (let i = 0; i < text.length; i++) {
          output[i] = text.charCodeAt(i);
        }
        return this.send(BLECommand.CMD_DISPLAY_TEXT, output);
      }
      /**
       * @param {Uint8Array} matrix - the matrix to display.
       * @return {Promise} - a Promise that resolves when writing to peripheral.
       */
      displayMatrix(matrix) {
        return this.send(BLECommand.CMD_DISPLAY_LED, matrix);
      }
      /**
       * @return {boolean} - the latest value received for the A button.
       */
      get buttonA() {
        return this._sensors.buttonA;
      }
      /**
       * @return {boolean} - the latest value received for the B button.
       */
      get buttonB() {
        return this._sensors.buttonB;
      }
      /**
       * @return {Uint8Array} - the current state of the 5x5 LED matrix.
       */
      get ledMatrixState() {
        return this._sensors.ledMatrixState;
      }
      /**
       * @return {number} - the latest value received for the amount of light falling on the LEDs.
       */
      get lightLevel() {
        return this._sensors.lightLevel;
      }
      /**
       * @return {number} - the angle (degrees) of heading direction from the north.
       */
      get compassHeading() {
        return this._sensors.compassHeading;
      }
      /**
       * @return {number} - the value of magnetic field strength [nano tesla].
       */
      get magneticStrength() {
        return this._sensors.magneticStrength;
      }
      /**
       * @return {number} - the value of gravitational acceleration [milli-g] for the X axis.
       */
      get accelerationX() {
        return 1e3 * this._sensors.accelerationX / G;
      }
      /**
       * @return {number} - the value of acceleration [milli-g] for the Y axis.
       */
      get accelerationY() {
        return 1e3 * this._sensors.accelerationY / G;
      }
      /**
       * @return {number} - the value of acceleration [milli-g] for the Z axis.
       */
      get accelerationZ() {
        return 1e3 * this._sensors.accelerationZ / G;
      }
      /**
       * Called by the runtime when user wants to scan for a peripheral.
       */
      scan() {
        if (this._ble) {
          this._ble.disconnect();
        }
        this._ble = new BLE(this._runtime, this._extensionId, {
          filters: [
            { services: [BLEUUID.service] }
          ]
        }, this._onConnect, this.reset);
      }
      /**
       * Called by the runtime when user wants to connect to a certain peripheral.
       * @param {number} id - the id of the peripheral to connect to.
       */
      connect(id) {
        if (this._ble) {
          this._ble.connectPeripheral(id);
        }
      }
      /**
       * Disconnect from the micro:bit.
       */
      disconnect() {
        if (this._ble) {
          this._ble.disconnect();
        }
        this.reset();
      }
      /**
       * Reset all the state and timeout/interval ids.
       */
      reset() {
        if (this._timeoutID) {
          window.clearTimeout(this._timeoutID);
          this._timeoutID = null;
        }
      }
      /**
       * Return true if connected to the micro:bit.
       * @return {boolean} - whether the micro:bit is connected.
       */
      isConnected() {
        let connected = false;
        if (this._ble) {
          connected = this._ble.isConnected();
        }
        return connected;
      }
      /**
       * Send a message to the peripheral BLE socket.
       * @param {number} command - the BLE command hex.
       * @param {Uint8Array} message - the message to write
       * @param {object} util - utility object provided by the runtime.
       */
      send(command, message, util) {
        if (!this.isConnected()) return;
        if (this._busy) {
          if (util) util.yield();
          return;
        }
        this._busy = true;
        this._busyTimeoutID = window.setTimeout(() => {
          this._busy = false;
        }, 5e3);
        const output = new Uint8Array(message.length + 1);
        output[0] = command;
        for (let i = 0; i < message.length; i++) {
          output[i + 1] = message[i];
        }
        const data = Base64Util.uint8ArrayToBase64(output);
        this._ble.write(BLEUUID.service, BLEUUID.txChar, data, "base64", true).then(
          () => {
            this._busy = false;
            window.clearTimeout(this._busyTimeoutID);
          }
        );
      }
      /**
       * Starts reading data from peripheral after BLE has connected to it.
       * @private
       */
      _onConnect() {
        this._ble.read(BLEUUID.service, BLEUUID.rxChar, true, this._onMessage);
        this._timeoutID = window.setTimeout(
          () => this._ble.handleDisconnectError(BLEDataStoppedError),
          BLETimeout
        );
        const dataView = new DataView(new ArrayBuffer(2));
        dataView.setUint16(0, 0, true);
      }
      /**
       * Process the sensor data from the incoming BLE characteristic.
       * @param {object} base64 - the incoming BLE data.
       * @private
       */
      _onMessage(base64) {
        const data = Base64Util.base64ToUint8Array(base64);
        this._sensors.buttonA = data[4];
        this._sensors.buttonB = data[5];
        const dataView = new DataView(data.buffer, 0);
        if (data[19] === 1) {
          this._sensors.analogValue[this.analogIn[0]] = dataView.getUint16(10, true);
          this._sensors.analogValue[this.analogIn[1]] = dataView.getUint16(12, true);
          this._sensors.analogValue[this.analogIn[2]] = dataView.getUint16(14, true);
          this._sensors.compassHeading = dataView.getUint16(16, true);
          this._sensors.lightLevel = dataView.getUint8(18);
        }
        if (data[19] === 2) {
          this._sensors.slot[0] = dataView.getInt16(10, true);
          this._sensors.slot[1] = dataView.getInt16(12, true);
          this._sensors.slot[2] = dataView.getInt16(14, true);
          this._sensors.slot[3] = dataView.getInt16(16, true);
          const gpioData = dataView.getUint8(18);
          for (let i = 0; i < this.gpio.length; i++) {
            this._sensors.digitalValue[this.gpio[i]] = gpioData >> i & 1;
          }
        }
        if (data[19] === 3) {
          this._sensors.magneticStrength = dataView.getUint16(10, true);
          this._sensors.accelerationX = dataView.getInt16(12, true);
          this._sensors.accelerationY = dataView.getInt16(14, true);
          this._sensors.accelerationZ = dataView.getInt16(16, true);
        }
        window.clearTimeout(this._timeoutID);
        this._timeoutID = window.setTimeout(
          () => this._ble.handleDisconnectError(BLEDataStoppedError),
          BLETimeout
        );
      }
      /**
       * Return the value of the slot.
       * @param {number} index - the slot index.
       * @return {number} - the latest value received for the slot.
       */
      getSlotValue(index) {
        return this._sensors.slot[index];
      }
      setSlotValue(slotIndex, slotValue, util) {
        const dataView = new DataView(new ArrayBuffer(2));
        dataView.setInt16(0, slotValue, true);
        this.send(
          BLECommand.CMD_SLOT_VALUE,
          new Uint8Array([
            slotIndex,
            dataView.getUint8(0),
            dataView.getUint8(1)
          ]),
          util
        );
        this._sensors.slot[slotIndex] = slotValue;
      }
    };
    var MicroBitButtons = {
      A: "A",
      B: "B",
      ANY: "any"
    };
    var AxisValues = {
      X: "x",
      Y: "y",
      Z: "z"
    };
    var LEDState = {
      ON: "on",
      OFF: "off"
    };
    var Scratch3Scratch2MaqueenBlocks = class _Scratch3Scratch2MaqueenBlocks {
      /**
       * @return {string} - the name of this extension.
       */
      static get EXTENSION_NAME() {
        return "Scratch2Maqueen";
      }
      /**
       * @return {string} - the ID of this extension.
       */
      static get EXTENSION_ID() {
        return "scratch2maqueen";
      }
      /**
       * @return {array} - text and values for each buttons menu element
       */
      get BUTTONS_MENU() {
        return [
          {
            text: "A",
            value: MicroBitButtons.A
          },
          {
            text: "B",
            value: MicroBitButtons.B
          },
          {
            text: formatMessage({
              id: "microbit.buttonsMenu.any",
              default: "any",
              description: 'label for "any" element in button picker for micro:bit extension'
            }),
            value: MicroBitButtons.ANY
          }
        ];
      }
      /**
       * @return {array} - text and values for LED state menu element
       */
      get LED_STATE_MENU() {
        return [
          {
            text: formatMessage({
              id: "scratch2maqueen.ledStateMenu.on",
              default: "on",
              description: "label for on element in LED state picker"
            }),
            value: LEDState.ON
          },
          {
            text: formatMessage({
              id: "scratch2maqueen.ledStateMenu.off",
              default: "off",
              description: "label for off element in LED state picker"
            }),
            value: LEDState.OFF
          }
        ];
      }
      get AXIS_MENU() {
        return [
          {
            text: "x",
            value: AxisValues.X
          },
          {
            text: "y",
            value: AxisValues.Y
          },
          {
            text: "z",
            value: AxisValues.Z
          }
        ];
      }
      /**
       * Construct a set of MicroBit blocks.
       * @param {Runtime} runtime - the Scratch 3.0 runtime.
       */
      constructor(runtime) {
        this.runtime = runtime;
        this._peripheral = new MicroBitMore(this.runtime, _Scratch3Scratch2MaqueenBlocks.EXTENSION_ID);
      }
      /**
       * @returns {object} metadata for this extension and its blocks.
       */
      getInfo() {
        this.setupTranslations();
        return {
          id: _Scratch3Scratch2MaqueenBlocks.EXTENSION_ID,
          name: _Scratch3Scratch2MaqueenBlocks.EXTENSION_NAME,
          blockIconURI,
          showStatusButton: true,
          blocks: [
            {
              opcode: "leftMotorSpeed",
              text: formatMessage({
                id: "scratch2maqueen.leftMotorSpeed",
                default: "Set left motor speed to [VALUE] (-255 - 255)",
                description: "set left motor speed"
              }),
              blockType: BlockType.COMMAND,
              arguments: {
                VALUE: {
                  type: ArgumentType.NUMBER,
                  defaultValue: 0
                }
              }
            },
            {
              opcode: "rightMotorSpeed",
              text: formatMessage({
                id: "scratch2maqueen.rightMotorSpeed",
                default: "Set right motor speed to [VALUE] (-255 - 255)",
                description: "set right motor speed"
              }),
              blockType: BlockType.COMMAND,
              arguments: {
                VALUE: {
                  type: ArgumentType.NUMBER,
                  defaultValue: 0
                }
              }
            },
            {
              opcode: "getDistance",
              text: formatMessage({
                id: "scratch2maqueen.getDistance",
                default: "Distance to obstacles ahead(cm)",
                description: "distance to obstacles ahead(cm)"
              }),
              blockType: BlockType.REPORTER
            },
            {
              opcode: "getPatrolLeft",
              text: formatMessage({
                id: "scratch2maqueen.getPatrolLeft",
                default: "Patrol Left",
                description: "value of patrol left"
              }),
              blockType: BlockType.REPORTER
            },
            {
              opcode: "getPatrolRight",
              text: formatMessage({
                id: "scratch2maqueen.getPatrolRight",
                default: "Patrol Right",
                description: "value of patrol right"
              }),
              blockType: BlockType.REPORTER
            },
            {
              opcode: "ledLeft",
              text: formatMessage({
                id: "scratch2maqueen.ledLeft",
                default: "Turn left LED [LED_STATE]",
                description: "turn left LED on/off"
              }),
              blockType: BlockType.COMMAND,
              arguments: {
                LED_STATE: {
                  type: ArgumentType.STRING,
                  menu: "ledState",
                  defaultValue: LEDState.ON
                }
              }
            },
            {
              opcode: "ledRight",
              text: formatMessage({
                id: "scratch2maqueen.ledRight",
                default: "Turn right LED [LED_STATE]",
                description: "turn right LED on/off"
              }),
              blockType: BlockType.COMMAND,
              arguments: {
                LED_STATE: {
                  type: ArgumentType.STRING,
                  menu: "ledState",
                  defaultValue: LEDState.ON
                }
              }
            },
            "---",
            {
              opcode: "whenButtonPressed",
              text: formatMessage({
                id: "microbit.whenButtonPressed",
                default: "when [BTN] button pressed",
                description: "when the selected button on the micro:bit is pressed"
              }),
              blockType: BlockType.HAT,
              arguments: {
                BTN: {
                  type: ArgumentType.STRING,
                  menu: "buttons",
                  defaultValue: MicroBitButtons.A
                }
              }
            },
            {
              opcode: "isButtonPressed",
              text: formatMessage({
                id: "microbit.isButtonPressed",
                default: "[BTN] button pressed?",
                description: "is the selected button on the micro:bit pressed?"
              }),
              blockType: BlockType.BOOLEAN,
              arguments: {
                BTN: {
                  type: ArgumentType.STRING,
                  menu: "buttons",
                  defaultValue: MicroBitButtons.A
                }
              }
            },
            "---",
            {
              opcode: "displaySymbol",
              text: formatMessage({
                id: "microbit.displaySymbol",
                default: "display [MATRIX]",
                description: "display a pattern on the micro:bit display"
              }),
              blockType: BlockType.COMMAND,
              arguments: {
                MATRIX: {
                  type: ArgumentType.MATRIX,
                  defaultValue: "0101010101100010101000100"
                }
              }
            },
            {
              opcode: "displayText",
              text: formatMessage({
                id: "microbit.displayText",
                default: "display text [TEXT]",
                description: "display text on the micro:bit display"
              }),
              blockType: BlockType.COMMAND,
              arguments: {
                TEXT: {
                  type: ArgumentType.STRING,
                  defaultValue: formatMessage({
                    id: "microbit.defaultTextToDisplay",
                    default: "Hello!",
                    description: `default text to display.
                                IMPORTANT - the micro:bit only supports letters a-z, A-Z.
                                Please substitute a default word in your language
                                that can be written with those characters,
                                substitute non-accented characters or leave it as "Hello!".
                                Check the micro:bit site documentation for details`
                  })
                }
              }
            },
            {
              opcode: "displayClear",
              text: formatMessage({
                id: "microbit.clearDisplay",
                default: "clear display",
                description: "display nothing on the micro:bit display"
              }),
              blockType: BlockType.COMMAND
            },
            "---",
            {
              opcode: "getLightLevel",
              text: formatMessage({
                id: "scratch2maqueen.lightLevel",
                default: "light intensity",
                description: "how much the amount of light falling on the LEDs on micro:bit"
              }),
              blockType: BlockType.REPORTER
            },
            {
              opcode: "getCompassHeading",
              text: formatMessage({
                id: "scratch2maqueen.compassHeading",
                default: "angle with the North",
                description: "angle from the North to the micro:bit heading direction"
              }),
              blockType: BlockType.REPORTER
            },
            {
              opcode: "getMagneticStrength",
              text: formatMessage({
                id: "scratch2maqueen.magneticForce",
                default: "magnetic force",
                description: "value of magnetic field strength (nano tesla)"
              }),
              blockType: BlockType.REPORTER
            },
            {
              opcode: "getAcceleration",
              text: formatMessage({
                id: "scratch2maqueen.acceleration",
                default: "acceleration [AXIS]",
                description: "value of acceleration on the axis (milli-g)"
              }),
              blockType: BlockType.REPORTER,
              arguments: {
                AXIS: {
                  type: ArgumentType.STRING,
                  menu: "axis",
                  defaultValue: AxisValues.X
                }
              }
            }
          ],
          menus: {
            buttons: {
              acceptReporters: true,
              items: this.BUTTONS_MENU
            },
            ledState: {
              acceptReporters: true,
              items: this.LED_STATE_MENU
            },
            axis: {
              acceptReporters: true,
              items: this.AXIS_MENU
            }
          }
        };
      }
      /**
       * Test whether the A or B button is pressed
       * @param {object} args - the block's arguments.
       * @return {boolean} - true if the button is pressed.
       */
      whenButtonPressed(args) {
        if (args.BTN === "any") {
          return this._peripheral.buttonA | this._peripheral.buttonB;
        } else if (args.BTN === "A") {
          return this._peripheral.buttonA;
        } else if (args.BTN === "B") {
          return this._peripheral.buttonB;
        }
        return false;
      }
      /**
       * Test whether the A or B button is pressed
       * @param {object} args - the block's arguments.
       * @return {boolean} - true if the button is pressed.
       */
      isButtonPressed(args) {
        if (args.BTN === "any") {
          return (this._peripheral.buttonA | this._peripheral.buttonB) !== 0;
        } else if (args.BTN === "A") {
          return this._peripheral.buttonA !== 0;
        } else if (args.BTN === "B") {
          return this._peripheral.buttonB !== 0;
        }
        return false;
      }
      /**
       * Display a predefined symbol on the 5x5 LED matrix.
       * @param {object} args - the block's arguments.
       * @return {Promise} - a Promise that resolves after a tick.
       */
      displaySymbol(args) {
        const symbol = cast.toString(args.MATRIX).replace(/\s/g, "");
        const reducer = (accumulator, c, index) => {
          const value = c === "0" ? accumulator : accumulator + Math.pow(2, index);
          return value;
        };
        const hex = symbol.split("").reduce(reducer, 0);
        if (hex !== null) {
          this._peripheral.ledMatrixState[0] = hex & 31;
          this._peripheral.ledMatrixState[1] = hex >> 5 & 31;
          this._peripheral.ledMatrixState[2] = hex >> 10 & 31;
          this._peripheral.ledMatrixState[3] = hex >> 15 & 31;
          this._peripheral.ledMatrixState[4] = hex >> 20 & 31;
          this._peripheral.displayMatrix(this._peripheral.ledMatrixState);
        }
        return new Promise((resolve) => {
          setTimeout(() => {
            resolve();
          }, BLESendInterval);
        });
      }
      /**
       * Display text on the 5x5 LED matrix.
       * @param {object} args - the block's arguments.
       * @return {Promise} - a Promise that resolves after the text is done printing.
       * Note the limit is 19 characters
       * The print time is calculated by multiplying the number of horizontal pixels
       * by the default scroll delay of 120ms.
       * The number of horizontal pixels = 6px for each character in the string,
       * 1px before the string, and 5px after the string.
       */
      displayText(args) {
        const text = String(args.TEXT).substring(0, 19);
        if (text.length > 0) this._peripheral.displayText(text);
        const yieldDelay = 120 * (6 * text.length + 6);
        return new Promise((resolve) => {
          setTimeout(() => {
            resolve();
          }, yieldDelay);
        });
      }
      /**
       * Turn all 5x5 matrix LEDs off.
       * @return {Promise} - a Promise that resolves after a tick.
       */
      displayClear() {
        for (let i = 0; i < 5; i++) {
          this._peripheral.ledMatrixState[i] = 0;
        }
        this._peripheral.displayMatrix(this._peripheral.ledMatrixState);
        return new Promise((resolve) => {
          setTimeout(() => {
            resolve();
          }, BLESendInterval);
        });
      }
      /**
       * Return amount of light on the LEDs.
       * @return {number} - the level of light amount (0 - 255).
       */
      getLightLevel() {
        return this._peripheral.lightLevel;
      }
      /**
       * Return angle from the north to the micro:bit heading direction.
       * @return {number} - the angle from the north (0 - 355 degrees).
       */
      getCompassHeading() {
        return this._peripheral.compassHeading;
      }
      // initialize (args, util) {
      //     this._peripheral.setPinPWM(0, 0, util);
      // }
      leftMotorSpeed(args, util) {
        const slotIndex = 0;
        const slotValue = parseInt(args.VALUE, 10);
        if (isNaN(slotValue)) return;
        this._peripheral.setSlotValue(slotIndex, slotValue, util);
      }
      rightMotorSpeed(args, util) {
        const slotIndex = 1;
        const slotValue = parseInt(args.VALUE, 10);
        if (isNaN(slotValue)) return;
        this._peripheral.setSlotValue(slotIndex, slotValue, util);
      }
      getDistance() {
        return this._peripheral.getSlotValue(2);
      }
      getPatrolLeft() {
        let slotValue = this._peripheral.getSlotValue(3);
        let bools = this.splitToBools(slotValue);
        return bools[2];
      }
      getPatrolRight() {
        let slotValue = this._peripheral.getSlotValue(3);
        let bools = this.splitToBools(slotValue);
        return bools[3];
      }
      ledLeft(args, util) {
        let slotValue = this._peripheral.getSlotValue(3);
        let bools = this.splitToBools(slotValue);
        if (args.LED_STATE == LEDState.ON) {
          bools[0] = 1;
        } else {
          bools[0] = 0;
        }
        this._peripheral.setSlotValue(3, this.joinBools(bools), util);
      }
      ledRight(args, util) {
        let slotValue = this._peripheral.getSlotValue(3);
        let bools = this.splitToBools(slotValue);
        if (args.LED_STATE == LEDState.ON) {
          bools[1] = 1;
        } else {
          bools[1] = 0;
        }
        this._peripheral.setSlotValue(3, this.joinBools(bools), util);
      }
      splitToBools(value) {
        let bools = [];
        bools[0] = Math.floor(value / 8);
        bools[1] = Math.floor((value - bools[0] * 8) / 4);
        bools[2] = Math.floor((value - bools[0] * 8 - bools[1] * 4) / 2);
        bools[3] = (value - bools[0] * 8 - bools[1] * 4) % 2;
        return bools;
      }
      joinBools(bools) {
        return bools[0] * 8 + bools[1] * 4 + bools[2] * 2 + bools[3];
      }
      /**
       * Return the value of magnetic field strength.
       * @return {number} - the value of magnetic field strength [nano tesla].
       */
      getMagneticStrength() {
        return this._peripheral.magneticStrength;
      }
      /**
       * Return the value of acceleration on the specified axis.
       * @param {object} args - the block's arguments.
       * @property {AxisValues} AXIS - the axis (X, Y, Z).
       * @return {number} - the value of acceleration on the axis [milli-g].
       */
      getAcceleration(args) {
        switch (args.AXIS) {
          case AxisValues.X:
            return this._peripheral.accelerationX;
          case AxisValues.Y:
            return this._peripheral.accelerationY;
          case AxisValues.Z:
            return this._peripheral.accelerationZ;
          default:
            log.warn(`Unknown axis in getAcceleration: ${args.AXIS}`);
        }
      }
      setupTranslations() {
        const localeSetup = formatMessage.setup();
        const extTranslations = {
          "ja": {
            "scratch2maqueen.lightLevel": "\u660E\u308B\u3055",
            "scratch2maqueen.compassHeading": "\u5317\u304B\u3089\u306E\u89D2\u5EA6",
            "scratch2maqueen.magneticForce": "\u78C1\u529B",
            "scratch2maqueen.acceleration": "\u52A0\u901F\u5EA6 [AXIS]",
            "scratch2maqueen.initialize": "\u521D\u671F\u5316\u3059\u308B",
            "scratch2maqueen.leftMotorSpeed": "\u5DE6\u30E2\u30FC\u30BF\u30FC\u306E\u901F\u3055\u3092 [VALUE] (-255\u301C255)\u306B\u3059\u308B",
            "scratch2maqueen.rightMotorSpeed": "\u53F3\u30E2\u30FC\u30BF\u30FC\u306E\u901F\u3055\u3092 [VALUE] (-255\u301C255)\u306B\u3059\u308B",
            "scratch2maqueen.getDistance": "\u524D\u65B9\u306E\u969C\u5BB3\u7269\u3068\u306E\u8DDD\u96E2(cm)",
            "scratch2maqueen.getPatrolLeft": "\u5DE6\u5074\u5E8A\u306E\u660E\u308B\u3055(\u9ED2:0, \u767D:1)",
            "scratch2maqueen.getPatrolRight": "\u53F3\u5074\u5E8A\u306E\u660E\u308B\u3055(\u9ED2:0, \u767D:1)",
            "scratch2maqueen.ledLeft": "\u5DE6\u5074LED [LED_STATE]",
            "scratch2maqueen.ledRight": "\u53F3\u5074LED [LED_STATE]",
            "scratch2maqueen.ledStateMenu.on": "\u5165",
            "scratch2maqueen.ledStateMenu.off": "\u5207"
          },
          "ja-Hira": {
            "scratch2maqueen.lightLevel": "\u3042\u304B\u308B\u3055",
            "scratch2maqueen.compassHeading": "\u304D\u305F\u304B\u3089\u306E\u304B\u304F\u3069",
            "scratch2maqueen.magneticForce": "\u3058\u308A\u3087\u304F",
            "scratch2maqueen.acceleration": "\u304B\u305D\u304F\u3069 [AXIS]",
            "scratch2maqueen.initialize": "\u3057\u3087\u304D\u304B\u3059\u308B",
            "scratch2maqueen.leftMotorSpeed": "\u3072\u3060\u308A\u30E2\u30FC\u30BF\u30FC\u306E\u306F\u3084\u3055\u3092 [VALUE] (-255\u301C255)\u306B\u3059\u308B",
            "scratch2maqueen.rightMotorSpeed": "\u307F\u304E\u30E2\u30FC\u30BF\u30FC\u306E\u306F\u3084\u3055\u3092 [VALUE] (-255\u301C255)\u306B\u3059\u308B",
            "scratch2maqueen.getDistance": "\u305C\u3093\u307D\u3046\u306E\u3057\u3087\u3046\u304C\u3044\u3076\u3064\u3068\u306E\u304D\u3087\u308A(cm)",
            "scratch2maqueen.getPatrolLeft": "\u3072\u3060\u308A\u304C\u308F\u3086\u304B\u306E\u3042\u304B\u308B\u3055(\u304F\u308D:0, \u3057\u308D:1)",
            "scratch2maqueen.getPatrolRight": "\u307F\u304E\u304C\u308F\u3086\u304B\u306E\u3042\u304B\u308B\u3055(\u304F\u308D:0, \u3057\u308D:1)",
            "scratch2maqueen.ledLeft": "\u3072\u3060\u308A\u304C\u308FLED [LED_STATE]",
            "scratch2maqueen.ledRight": "\u307F\u304E\u304C\u308FLED [LED_STATE]",
            "scratch2maqueen.ledStateMenu.on": "\u3044\u308A",
            "scratch2maqueen.ledStateMenu.off": "\u304D\u308A"
          }
        };
        for (const locale in extTranslations) {
          if (!localeSetup.translations[locale]) {
            localeSetup.translations[locale] = {};
          }
          Object.assign(localeSetup.translations[locale], extTranslations[locale]);
        }
      }
    };
    module.exports = Scratch3Scratch2MaqueenBlocks;
  }
});

// .cache/stretch3-extensions/work/entries/scratch2maqueen.js
var import_scratch3_scratch2maqueen = __toESM(require_scratch3_scratch2maqueen());
var BlockClass = import_scratch3_scratch2maqueen.default?.default ?? import_scratch3_scratch2maqueen.default;
var blockClass = BlockClass;
var entry = {
  extensionId: "scratch2maqueen",
  name: "Scratch2Maqueen"
};
export {
  blockClass,
  entry
};
