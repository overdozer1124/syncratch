/* Syncratch bundled Stretch3 extension: pasorich
 * Upstream: https://github.com/con3code/pasorich.git
 * Regenerated by: node scripts/stretch3-extensions/bundle.mjs
 */

var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __commonJS = (cb, mod) => function __require() {
  try {
    return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
  } catch (e) {
    throw mod = 0, e;
  }
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));

// vendor/scratch-editor/packages/scratch-vm/src/extension-support/argument-type.js
var require_argument_type = __commonJS({
  "vendor/scratch-editor/packages/scratch-vm/src/extension-support/argument-type.js"(exports, module) {
    var ArgumentType = {
      /**
       * Numeric value with angle picker
       */
      ANGLE: "angle",
      /**
       * Boolean value with hexagonal placeholder
       */
      BOOLEAN: "Boolean",
      /**
       * Numeric value with color picker
       */
      COLOR: "color",
      /**
       * Numeric value with text field
       */
      NUMBER: "number",
      /**
       * String value with text field
       */
      STRING: "string",
      /**
       * String value with matrix field
       */
      MATRIX: "matrix",
      /**
       * MIDI note number with note picker (piano) field
       */
      NOTE: "note",
      /**
       * Inline image on block (as part of the label)
       */
      IMAGE: "image"
    };
    module.exports = ArgumentType;
  }
});

// vendor/scratch-editor/packages/scratch-vm/src/extension-support/block-type.js
var require_block_type = __commonJS({
  "vendor/scratch-editor/packages/scratch-vm/src/extension-support/block-type.js"(exports, module) {
    var BlockType = {
      /**
       * Boolean reporter with hexagonal shape
       */
      BOOLEAN: "Boolean",
      /**
       * A button (not an actual block) for some special action, like making a variable
       */
      BUTTON: "button",
      /**
       * Command block
       */
      COMMAND: "command",
      /**
       * Specialized command block which may or may not run a child branch
       * The thread continues with the next block whether or not a child branch ran.
       */
      CONDITIONAL: "conditional",
      /**
       * Specialized hat block with no implementation function
       * This stack only runs if the corresponding event is emitted by other code.
       */
      EVENT: "event",
      /**
       * Hat block which conditionally starts a block stack
       */
      HAT: "hat",
      /**
       * Specialized command block which may or may not run a child branch
       * If a child branch runs, the thread evaluates the loop block again.
       */
      LOOP: "loop",
      /**
       * General reporter with numeric or string value
       */
      REPORTER: "reporter"
    };
    module.exports = BlockType;
  }
});

// scripts/stretch3-extensions/shims/format-message-setup.cjs
var require_format_message_setup = __commonJS({
  "scripts/stretch3-extensions/shims/format-message-setup.cjs"(exports, module) {
    var formatMessage = require_format_message_setup();
    try {
      formatMessage.setup({
        locale: "ja",
        missingTranslation: "ignore"
      });
    } catch {
    }
    module.exports = formatMessage;
  }
});

// .cache/stretch3-extensions/work/src/extensions/scratch3_pasorich/index.js
var require_scratch3_pasorich = __commonJS({
  ".cache/stretch3-extensions/work/src/extensions/scratch3_pasorich/index.js"(exports, module) {
    var ArgumentType = require_argument_type();
    var BlockType = require_block_type();
    var formatMessage = require_format_message_setup();
    var pasoriDevice = {
      443: {
        vendorId: 1356,
        productId: 443,
        modelType: "320",
        modelName: "RC-S320",
        endpointInNum: "",
        endpointOutNum: ""
      },
      737: {
        vendorId: 1356,
        productId: 737,
        modelType: "330",
        modelName: "RC-S330",
        endpointInNum: "",
        endpointOutNum: ""
      },
      1729: {
        vendorId: 1356,
        productId: 1729,
        modelType: "380S",
        modelName: "RC-S380/S",
        endpointInNum: "",
        endpointOutNum: ""
      },
      1731: {
        vendorId: 1356,
        productId: 1731,
        modelType: "380P",
        modelName: "RC-S380/P",
        endpointInNum: "",
        endpointOutNum: ""
      },
      3528: {
        vendorId: 1356,
        productId: 3528,
        modelType: "300S",
        modelName: "RC-S300/S",
        endpointInNum: "",
        endpointOutNum: ""
      },
      3529: {
        vendorId: 1356,
        productId: 3529,
        modelType: "380P",
        modelName: "RC-S300/P",
        endpointInNum: "",
        endpointOutNum: ""
      }
    };
    var nfcDevices = [];
    var deviceOpening = false;
    var seqNumber = 0;
    var pasorichVersion = "PaSoRich 2.0(2004)";
    var blockIconURI = "data:image/svg+xml;base64,";
    var menuIconURI = "data:image/svg+xml;base64,";
    var EXTENSION_ID = "pasorich";
    var Scratch3PasorichBlocks = class _Scratch3PasorichBlocks {
      /**
      * @return {string} - the name of this extension.
      */
      static get EXTENSION_NAME() {
        return formatMessage({
          id: "pasorich.name",
          default: "PaSoRich",
          description: "name of the extension"
        }).toString();
      }
      /**
       * @return {string} - the ID of this extension.
       */
      static get EXTENSION_ID() {
        return EXTENSION_ID;
      }
      /**
       * URL to get this extension.
       * @type {string}
       */
      static get extensionURL() {
        return extensionURL;
      }
      /**
       * Set URL to get this extension.
       * The extensionURL will be changed to the URL of the loading server.
       * @param {string} url - URL
       */
      static set extensionURL(url) {
        extensionURL = url;
      }
      constructor(runtime) {
        this.runtime = runtime;
        this.whenReadCountMap = /* @__PURE__ */ new Map();
        this.allHatBlocksDone_flag = false;
        console.log(pasorichVersion);
        if (runtime.formatMessage) {
          formatMessage = runtime.formatMessage;
        }
      }
      openPasori() {
        if (deviceOpening) {
          deviceWaiter(1);
        }
        if (nfcDevices.length > 0) {
          isConnect = formatMessage({
            id: "pasorich.ConnectConnected",
            default: "Connected...",
            description: "ConnectConnected"
          });
        } else {
          isConnect = formatMessage({
            id: "pasorich.ConnectConnecting",
            default: "Connecting...",
            description: "ConnectConnecting"
          });
        }
        return new Promise((resolve, reject) => {
          deviceOpening = true;
          navigator.usb.requestDevice({ filters: [{ vendorId: 1356 }] }).then((device) => {
            const existingDevice = nfcDevices.find((d) => d && d.device.serialNumber === device.serialNumber);
            if (existingDevice) {
              deviceOpening = false;
              resolve(false);
            } else {
              addNfcDevice(device);
              this.getDeviceNumberMenuItems();
              isConnect = formatMessage({
                id: "pasorich.ConnectConnected",
                default: "Connected...",
                description: "ConnectConnected"
              });
              deviceOpening = false;
              resolve(isConnect);
            }
          }).catch((error) => {
            deviceOpening = false;
            reject(error);
          });
          this.getDeviceNumberMenuItems();
          resolve(isConnect);
        });
      }
      /*
      
          readPasori(args) // -> Scratch3PasorichBlocks.prototype.readPasori
      
      */
      getIdm(args) {
        return getIdmNum(args.DEVICE_NUMBER);
      }
      resetIdm() {
        return new Promise((resolve, reject) => {
          if (nfcDevices.length != 0) {
            nfcDevices.forEach(async (nfc) => {
              nfc.idmNum = "";
            });
            console.log("resetIdm");
          }
          resolve();
        });
      }
      resetDevice() {
        return new Promise((resolve, reject) => {
          if (nfcDevices.length != 0) {
            nfcDevices.forEach(async (nfc) => {
              nfc.idmNum = "";
              let confValue = nfc.device.configurations[0].configurationValue || 1;
              let interfaceNum = nfc.device.configurations[0].interfaces[confValue - 1].interfaceNumber || 0;
              await nfc.device.releaseInterface(interfaceNum);
              await nfc.device.close();
            });
            nfcDevices.splice(0, nfcDevices.length);
            this.getDeviceNumberMenuItems();
            console.log("resetDevices");
          }
          resolve();
        });
      }
      /*
      
          whenRead(args, util) // -> Scratch3PasorichBlocks.prototype.whenRead
      
      */
      /**
       * @returns {object} metadata for this extension and its blocks.
       */
      getInfo() {
        this.setupTranslations();
        return {
          id: _Scratch3PasorichBlocks.EXTENSION_ID,
          name: _Scratch3PasorichBlocks.EXTENSION_NAME,
          // 'PaSoRich',
          menuIconURI,
          blockIconURI,
          showStatusButton: false,
          color1: "#608DD3",
          color2: "#608DD3",
          blocks: [
            {
              opcode: "openPasori",
              text: formatMessage({
                id: "pasorich.Connect",
                default: "Connect",
                description: "openPasori"
              }),
              blockType: BlockType.COMMAND
            },
            "---",
            {
              opcode: "readPasori",
              text: formatMessage({
                id: "pasorich.readPasori",
                default: "read #[DEVICE_NUMBER]reader",
                description: "readPasori"
              }),
              blockType: BlockType.COMMAND,
              arguments: {
                DEVICE_NUMBER: {
                  type: ArgumentType.STRING,
                  menu: "deviceNumberMenu",
                  defaultValue: "1"
                  // デフォルトのデバイス番号
                }
              }
            },
            {
              opcode: "getIdm",
              text: formatMessage({
                id: "pasorich.getIdm",
                default: "IDm of #[DEVICE_NUMBER]",
                description: "getIDm"
              }),
              blockType: BlockType.REPORTER,
              arguments: {
                DEVICE_NUMBER: {
                  type: ArgumentType.STRING,
                  menu: "deviceNumberMenu",
                  defaultValue: "1"
                  // デフォルトのデバイス番号
                }
              }
            },
            "---",
            {
              opcode: "resetIdm",
              text: formatMessage({
                id: "pasorich.resetIdm",
                default: "reset IDm",
                description: "reset IDm Variables"
              }),
              blockType: BlockType.COMMAND
            },
            {
              opcode: "resetDevice",
              text: formatMessage({
                id: "pasorich.resetDevice",
                default: "reset Device",
                description: "reset Devices"
              }),
              blockType: BlockType.COMMAND
            },
            "---",
            {
              opcode: "whenRead",
              blockType: BlockType.HAT,
              text: formatMessage({
                id: "pasorich.whenRead",
                default: "when read #[DEVICE_NUMBER]reader",
                description: "whenRead"
              }),
              arguments: {
                DEVICE_NUMBER: {
                  type: ArgumentType.STRING,
                  menu: "deviceNumberMenu",
                  defaultValue: "1"
                  // デフォルトのデバイス番号
                }
              }
            }
          ],
          menus: {
            deviceNumberMenu: {
              acceptReporters: true,
              items: "getDeviceNumberMenuItems"
            }
          }
        };
      }
      // デバイス番号メニューの項目を生成する関数
      getDeviceNumberMenuItems() {
        if (nfcDevices.length === 0) {
          return [{
            text: " ",
            value: " "
          }];
        }
        return nfcDevices.map((_, index) => ({
          text: (index + 1).toString(),
          value: (index + 1).toString()
        }));
      }
      setupTranslations() {
        const localeSetup = formatMessage.setup();
        const extensionTranslations = {
          "en": {
            "pasorich.name": "PaSoRich",
            "pasorich.push2Connect": "Push to Connect.",
            "pasorich.Connect": "Connect",
            "pasorich.readPasori": "read #[DEVICE_NUMBER]reader",
            "pasorich.getIdm": "IDm of #[DEVICE_NUMBER]",
            "pasorich.getHashedIdm": "HexIDm",
            "pasorich.resetIdm": "reset IDm",
            "pasorich.resetDevice": "reset Device",
            "pasorich.whenRead": "when read #[DEVICE_NUMBER]reader",
            "pasorich.getReadingFlag": "reading",
            "pasorich.getWaitingFlag": "waiting",
            "pasorich.readingDone": "reading done",
            "pasorich.ConnectReading": "reading...",
            "pasorich.ConnectConnected": "connected...",
            "pasorich.ConnectConnecting": "connecting...",
            "pasorich.ConnectSuccess": "connected...",
            "pasorich.ConnectFailure": "connection failed..."
          },
          "ja": {
            "pasorich.name": "\u30D1\u30BD\u30EA\u30C3\u30C1",
            "pasorich.push2Connect": "\u62BC\u3057\u3066\u63A5\u7D9A",
            "pasorich.Connect": "\u63A5\u7D9A",
            "pasorich.readPasori": "[DEVICE_NUMBER]\u756A\u306E\u8AAD\u307F\u53D6\u308A",
            "pasorich.getIdm": "[DEVICE_NUMBER]\u756A\u306EIDm",
            "pasorich.getHashedIdm": "HexIDm",
            "pasorich.resetIdm": "IDm\u30EA\u30BB\u30C3\u30C8",
            "pasorich.resetDevice": "\u30C7\u30D0\u30A4\u30B9\u30EA\u30BB\u30C3\u30C8",
            "pasorich.whenRead": "[DEVICE_NUMBER]\u756A\u304C\u8AAD\u307F\u53D6\u3089\u308C\u305F\u3068\u304D",
            "pasorich.getReadingFlag": "\u8AAD\u53D6\u4E2D",
            "pasorich.getWaitingFlag": "\u5F85\u6A5F\u4E2D",
            "pasorich.readingDone": "\u8AAD\u307F\u53D6\u308A\u5B8C\u4E86",
            "pasorich.ConnectReading": "\u8AAD\u53D6\u4E2D...",
            "pasorich.ConnectConnected": "\u63A5\u7D9A\u6E08\u307F...",
            "pasorich.ConnectConnecting": "\u63A5\u7D9A\u4E2D...",
            "pasorich.ConnectSuccess": "\u63A5\u7D9A\u6210\u529F...",
            "pasorich.ConnectFailure": "\u63A5\u7D9A\u5931\u6557..."
          },
          "ja-Hira": {
            "pasorich.name": "\u3071\u305D\u308A\u3063\u3061",
            "pasorich.push2Connect": "\u304A\u3057\u3066\u305B\u3064\u305E\u304F",
            "pasorich.Connect": "\u305B\u3064\u305E\u304F",
            "pasorich.readPasori": "[DEVICE_NUMBER]\u3070\u3093\u306E\u3088\u307F\u3068\u308A",
            "pasorich.getIdm": "[DEVICE_NUMBER]\u3070\u3093\u306EIDm",
            "pasorich.getHashedIdm": "HexIDm",
            "pasorich.resetIdm": "IDm\u30EA\u30BB\u30C3\u30C8",
            "pasorich.resetDevice": "\u3067\u3070\u3044\u3059\u30EA\u30BB\u30C3\u30C8",
            "pasorich.whenRead": "[DEVICE_NUMBER]\u3070\u3093\u304C\u3088\u307F\u3068\u3089\u308C\u305F\u3068\u304D",
            "pasorich.getReadingFlag": "\u3088\u307F\u3068\u308A\u3061\u3085\u3046",
            "pasorich.getWaitingFlag": "\u305F\u3044\u304D\u3061\u3085\u3046",
            "pasorich.readingDone": "\u3088\u307F\u3068\u308A\u304B\u3093\u308A\u3087\u3046",
            "pasorich.ConnectReading": "\u3088\u307F\u3068\u308A\u3061\u3085\u3046...",
            "pasorich.ConnectConnected": "\u305B\u3064\u305E\u304F\u305A\u307F...",
            "pasorich.ConnectConnecting": "\u305B\u3064\u305E\u304F\u3061\u3085\u3046...",
            "pasorich.ConnectSuccess": "\u305B\u3064\u305E\u304F\u305B\u3044\u3053\u3046...",
            "pasorich.ConnectFailure": "\u305B\u3064\u305E\u304F\u3057\u3063\u3071\u3044..."
          }
        };
        for (const locale in extensionTranslations) {
          if (!localeSetup.translations[locale]) {
            localeSetup.translations[locale] = {};
          }
          Object.assign(localeSetup.translations[locale], extensionTranslations[locale]);
        }
      }
    };
    var AsyncQueue = class {
      constructor() {
        this.queue = [];
        this.pendingPromise = false;
      }
      async enqueue(task) {
        return new Promise((resolve, reject) => {
          this.queue.push(() => task().then(resolve).catch(reject));
          if (!this.pendingPromise) {
            this.pendingPromise = true;
            this.dequeue();
          }
        });
      }
      async dequeue() {
        if (this.queue.length === 0) {
          this.pendingPromise = false;
          return;
        }
        const task = this.queue.shift();
        try {
          await task();
        } catch (e) {
          console.error("Error during async task execution:", e);
        } finally {
          this.dequeue();
        }
      }
    };
    var isConnect = formatMessage({
      id: "pasorich.push2Connect",
      default: "Push to Connect.",
      description: "push2Connect"
    });
    var getEndpoint = (argInterface, argValue) => {
      let retValue = false;
      for (const val of argInterface.alternate.endpoints) {
        if (val.direction == argValue && val.type == "bulk") {
          console.log(val);
          retValue = val;
        }
      }
      return retValue;
    };
    async function setupDevice(device) {
      const modelType = device.productId;
      const pasoriDeviceModel = pasoriDevice[modelType];
      let deviceInterface;
      console.log("setupDevice:", device);
      const confValue = device.configurations[0].configurationValue || 1;
      console.log("configurationValue:", confValue);
      if (device.productId === 3528 || device.productId === 3529) {
        deviceInterface = device.configuration.interfaces.filter((v) => v.alternate.interfaceClass == 255)[0];
        pasoriDeviceModel.endPointInNum = deviceInterface.alternate.endpoints.filter((e) => e.direction == "in")[0].endpointNumber;
        pasoriDeviceModel.endPointOutNum = deviceInterface.alternate.endpoints.filter((e) => e.direction == "out")[0].endpointNumber;
      }
      if (device.productId === 1729 || device.productId === 1731) {
        deviceInterface = device.configurations[0].interfaces[0];
        let deviceEndpoint = await getEndpoint(deviceInterface, "in");
        pasoriDeviceModel.endPointInNum = deviceEndpoint.endpointNumber;
        deviceEndpoint = await getEndpoint(deviceInterface, "out");
        pasoriDeviceModel.endPointOutNum = deviceEndpoint.endpointNumber;
      }
      console.log("interfaceNumber:", deviceInterface.interfaceNumber);
      try {
        await device.open();
        await device.selectConfiguration(confValue);
        await device.claimInterface(deviceInterface.interfaceNumber);
      } catch (error) {
        console.error("This device is currently in use or down:", error);
      }
      return device;
    }
    var readPasoriQueue = new AsyncQueue();
    Scratch3PasorichBlocks.prototype.readPasoriTask = function(deviceNumber) {
      return new Promise((resolve, reject) => {
        if (deviceNumber > 0 && deviceNumber <= nfcDevices.length) {
          const device = getNfcDeviceByNumber(deviceNumber);
          if (device) {
            if (device.opened) {
              session(device).then(resolve).catch(reject);
            } else {
              setupDevice(device).then(() => session(device)).then(resolve).catch((error) => {
                console.error("Failed to setup or session the device:", error);
                reject(error);
              });
            }
          } else {
            console.error("Invalid device number");
            reject("Invalid device");
          }
        } else {
          navigator.usb.requestDevice({ filters: [{ vendorId: 1356 }] }).then((device) => {
            const existingDevice = nfcDevices.find((d) => d && d.device.serialNumber === device.serialNumber);
            if (existingDevice) {
            } else {
              addNfcDevice(device);
              this.getDeviceNumberMenuItems();
              isConnect = formatMessage({
                id: "pasorich.ConnectConnected",
                default: "Connected...",
                description: "ConnectConnected"
              });
            }
          }).then(() => {
            const device = getNfcDeviceByNumber(deviceNumber);
            if (device) {
              if (device.opened) {
                session(device).then(resolve).catch(reject);
              } else {
                setupDevice(device).then(() => session(device)).then(resolve).catch((error) => {
                  console.error("Failed to setup or session the device:", error);
                  reject(error);
                });
              }
            } else {
              console.error("Invalid device number");
              reject("Invalid device");
            }
          }).catch((error) => {
            reject(error);
          });
          this.getDeviceNumberMenuItems();
        }
      }).then(() => {
        this.pasoriReadCallback(deviceNumber);
      });
    };
    Scratch3PasorichBlocks.prototype.readPasori = function(args) {
      const deviceNumber = args.DEVICE_NUMBER;
      if (deviceNumber <= 0 && deviceNumber > nfcDevices.length + 1) {
        return;
      }
      return readPasoriQueue.enqueue(() => {
        return this.readPasoriTask(deviceNumber);
      });
    };
    Scratch3PasorichBlocks.prototype.pasoriReadCallback = function(deviceNumber) {
      this.whenReadCountMap.forEach((readList, blockId) => {
        if (!Array.isArray(readList)) {
          readList = [];
          this.whenReadCountMap.set(blockId, readList);
        }
        readList.push(deviceNumber);
      });
    };
    Scratch3PasorichBlocks.prototype.whenReadCalled = function(blockId, deviceNo) {
      const readList = this.whenReadCountMap.get(blockId) || [];
      if (readList.length > 0) {
        let deviceNumber = readList[0];
        readList.shift();
        this.whenReadCountMap.set(blockId, readList);
        return deviceNumber == deviceNo;
      } else {
        this.whenReadCountMap.set(blockId, readList);
      }
      return false;
    };
    Scratch3PasorichBlocks.prototype.whenRead = function(args, util) {
      const blockId = util.thread.topBlock;
      const deviceNo = args.DEVICE_NUMBER;
      return this.whenReadCalled(blockId, deviceNo);
    };
    async function send300(device, endpointOut, data) {
      let uint8data = new Uint8Array(data);
      const dataLength = uint8data.length;
      const SLOTNUMBER = 0;
      let reqPckt = new Uint8Array(10 + dataLength);
      reqPckt[0] = 107;
      reqPckt[1] = 255 & dataLength;
      reqPckt[2] = dataLength >> 8 & 255;
      reqPckt[3] = dataLength >> 16 & 255;
      reqPckt[4] = dataLength >> 24 & 255;
      reqPckt[5] = SLOTNUMBER;
      reqPckt[6] = ++seqNumber;
      0 != dataLength && reqPckt.set(uint8data, 10);
      await device.transferOut(endpointOut, reqPckt);
      await sleep(20);
    }
    async function send(device, endpointOut, data) {
      let uint8data = new Uint8Array(data);
      await device.transferOut(endpointOut, uint8data);
      await sleep(10);
    }
    async function receive(device, endpointIn, len) {
      let data = await device.transferIn(endpointIn, len);
      await sleep(10);
      let arr = [];
      for (let i = data.data.byteOffset; i < data.data.byteLength; i++) {
        arr.push(data.data.getUint8(i));
      }
      return arr;
    }
    function padding_zero(num, p) {
      return ("0".repeat(p * 1) + num).slice(-(p * 1));
    }
    function dec2HexString(n) {
      return padding_zero((n * 1).toString(16), 2);
    }
    async function session(device) {
      let pasoriDeviceModel = pasoriDevice[device.productId];
      let endpointOut = pasoriDeviceModel.endPointOutNum;
      let endpointIn = pasoriDeviceModel.endPointInNum;
      if (device.productId === 3528 || device.productId === 3529) {
        const len = 50;
        await send300(device, endpointOut, [255, 80, 0, 0, 2, 130, 0, 0]);
        await receive(device, endpointIn, len);
        await send300(device, endpointOut, [255, 80, 0, 0, 2, 129, 0, 0]);
        await receive(device, endpointIn, len);
        await send300(device, endpointOut, [255, 80, 0, 0, 2, 131, 0, 0]);
        await receive(device, endpointIn, len);
        await send300(device, endpointOut, [255, 80, 0, 0, 2, 132, 0, 0]);
        await receive(device, endpointIn, len);
        await send300(device, endpointOut, [255, 80, 0, 2, 4, 143, 2, 3, 0, 0]);
        await receive(device, endpointIn, len);
        await send300(device, endpointOut, [255, 80, 0, 1, 0, 0, 17, 95, 70, 4, 160, 134, 1, 0, 149, 130, 0, 6, 6, 0, 255, 255, 1, 0, 0, 0, 0]);
        const poling_res_f = await receive(device, endpointIn, len);
        if (poling_res_f.length == 46) {
          let idmStr = "";
          let idm = poling_res_f.slice(26, 34).map((v) => dec2HexString(v));
          idmStr = idm.join("");
          let idmNum = JSON.parse(JSON.stringify(idmStr));
          await setIdmNum(device, idmNum);
          return;
        } else {
          await setIdmNum(device, "");
        }
      }
      if (device.productId === 1729 || device.productId === 1731) {
        await send(device, endpointOut, [0, 0, 255, 0, 255, 0]);
        await send(device, endpointOut, [0, 0, 255, 255, 255, 3, 0, 253, 214, 42, 1, 255, 0]);
        await receive(device, endpointIn, 6);
        await receive(device, endpointIn, 13);
        await send(device, endpointOut, [0, 0, 255, 255, 255, 3, 0, 253, 214, 6, 0, 36, 0]);
        await receive(device, endpointIn, 6);
        await receive(device, endpointIn, 13);
        await send(device, endpointOut, [0, 0, 255, 255, 255, 3, 0, 253, 214, 6, 0, 36, 0]);
        await receive(device, endpointIn, 6);
        await receive(device, endpointIn, 13);
        await send(device, endpointOut, [0, 0, 255, 255, 255, 6, 0, 250, 214, 0, 1, 1, 15, 1, 24, 0]);
        await receive(device, endpointIn, 6);
        await receive(device, endpointIn, 13);
        await send(device, endpointOut, [0, 0, 255, 255, 255, 40, 0, 216, 214, 2, 0, 24, 1, 1, 2, 1, 3, 0, 4, 0, 5, 0, 6, 0, 7, 8, 8, 0, 9, 0, 10, 0, 11, 0, 12, 0, 14, 4, 15, 0, 16, 0, 17, 0, 18, 0, 19, 6, 75, 0]);
        await receive(device, endpointIn, 6);
        await receive(device, endpointIn, 13);
        await send(device, endpointOut, [0, 0, 255, 255, 255, 4, 0, 252, 214, 2, 0, 24, 16, 0]);
        await receive(device, endpointIn, 6);
        await receive(device, endpointIn, 13);
        await send(device, endpointOut, [0, 0, 255, 255, 255, 10, 0, 246, 214, 4, 110, 0, 6, 0, 255, 255, 1, 0, 179, 0]);
        await receive(device, endpointIn, 6);
        let idm = (await receive(device, endpointIn, 37)).slice(17, 25);
        if (idm.length > 0) {
          let idmStr = "";
          for (let i = 0; i < idm.length; i++) {
            if (idm[i] < 16) {
              idmStr += "0";
            }
            idmStr += idm[i].toString(16);
          }
          let idmNum = JSON.parse(JSON.stringify(idmStr));
          await setIdmNum(device, idmNum);
        } else {
          await setIdmNum(device, "");
        }
      }
    }
    function getIdmNum(deviceNumber) {
      if (deviceNumber > 0 && deviceNumber <= nfcDevices.length) {
        return nfcDevices[deviceNumber - 1].idmNum;
      }
      return null;
    }
    function setIdmNum(device, idmNum) {
      const deviceIndex = nfcDevices.findIndex((d) => d.device && d.device.serialNumber === device.serialNumber);
      console.log("IDm #", deviceIndex + 1, ": ", idmNum, "(", device.productName, ":", device.serialNumber, ")");
      if (deviceIndex !== -1) {
        nfcDevices[deviceIndex].idmNum = idmNum;
      }
    }
    function addNfcDevice(device) {
      if (device instanceof USBDevice) {
        const existingDevice = nfcDevices.find((d) => d && d.serialNumber === device.serialNumber);
        if (existingDevice) {
          return false;
        }
        nfcDevices.push({ device, idmNum: "" });
        return true;
      } else {
        console.error("The provided object is not a USBDevice instance.");
      }
    }
    function getNfcDeviceByNumber(deviceNumber) {
      if (deviceNumber > 0 && deviceNumber <= nfcDevices.length) {
        return nfcDevices[deviceNumber - 1].device;
      }
    }
    function sleep(msec) {
      return new Promise(
        (resolve) => setTimeout(() => {
          resolve();
        }, msec)
      );
    }
    function deviceWaiter(msec) {
      return new Promise(
        (resolve, reject) => setTimeout(() => {
          if (!deviceOpening) {
            reject();
          } else {
            resolve();
          }
        }, msec)
      ).catch(() => {
        return deviceWaiter(msec);
      });
    }
    module.exports = Scratch3PasorichBlocks;
  }
});

// .cache/stretch3-extensions/work/entries/pasorich.js
var import_scratch3_pasorich = __toESM(require_scratch3_pasorich());
var BlockClass = import_scratch3_pasorich.default?.default ?? import_scratch3_pasorich.default;
var blockClass = BlockClass;
var entry = {
  extensionId: "pasorich",
  name: "PaSoRich 2.0"
};
export {
  blockClass,
  entry
};
