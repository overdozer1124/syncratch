{
  "name": "@blocksync/collab-webrtc",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "EXPERIMENTAL browser-only Yjs/WebRTC collaboration transport. Configurable signaling URL only, never a public signaling fallback. Encrypts payloads with the room secret. No R1 API stability.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@blocksync/collab-invite": "workspace:*",
    "yjs": "^13.6.27"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
