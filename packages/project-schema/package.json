{
  "name": "@blocksync/project-schema",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "EXPERIMENTAL Gate 0 project document model and structure invariants. No R1 API stability.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
