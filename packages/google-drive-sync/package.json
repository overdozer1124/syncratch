{
  "name": "@blocksync/google-drive-sync",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Browser-only Google Drive .sb3 synchronization.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:browser": "vite build",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vite": "^7.3.6",
    "vitest": "^3.2.4"
  }
}
