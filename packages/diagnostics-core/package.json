{
  "name": "@blocksync/diagnostics-core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "EXPERIMENTAL browser-local standard diagnostics: DiagnosticProjectIR, rule findings, no network/Transformers.js. No R1 API stability.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@blocksync/project-schema": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
