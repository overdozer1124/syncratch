{
  "name": "@blocksync/classroom-apps-script",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Optional browser-safe client and contract for the BlockSync Apps Script classroom control plane.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
