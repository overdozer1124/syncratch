{
  "name": "@blocksync/collab-leader",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "EXPERIMENTAL deterministic best-effort leader election and leadership epoch derivation for small-room collaboration. No R1 API stability.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
