{
  "name": "@blocksync/classroom-access",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "EXPERIMENTAL classroom admin/student access: policy types, student link tokens, surface mode. No School roster/RBAC. No R1 API stability.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
