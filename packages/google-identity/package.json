{
  "name": "@blocksync/google-identity",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "EXPERIMENTAL Gate 0 Google ID token verification (authn only). No R1 API stability.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "jose": "^6.0.11"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
